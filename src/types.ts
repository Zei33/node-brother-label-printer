/**
 * Main types export for backward compatibility
 * 
 * This file now serves as a compatibility layer, re-exporting types from the new
 * organized type structure. The actual type definitions have been moved to the
 * types/ directory for better organization.
 */

// Re-export all types from the new structure
export * from './types/index.js';